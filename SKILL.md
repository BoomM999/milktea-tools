---
name: milktea-recipe-import
description: 将奶茶前场配料 PDF（"前场配料" 内部文件）转换为奶茶机可导入的 .xlsx 文件。当用户提供 PDF 配料表并要求生成"导入配方"Excel 时，触发本技能。覆盖文本提取、表格解析、PDF→Excel 字段映射、温度×糖度组合展开、用量归一化等步骤。
agent_created: true
---

# Milktea Recipe Import (PDF → Excel)

## Overview

本技能把奶茶前场配料 PDF 转换为导入配方 Excel（奶茶机专用导入格式）。
入口是 PDF（标题"前场配料"，内部文件，通常 1-10 页表格，多个品牌分组），输出是 .xlsx（9 列：版本号/饮品名称/杯型/做法/温度/糖度/原料名称/原料用量/原料排序）。

## When To Use

- 用户说"按这份 PDF 给我生成导入配方" / "把配料表转成 Excel" / "出奶茶机导入文件"。
- 用户丢过来一个标题为"前场配料"的 PDF。

## Workflow

1. **解析 PDF**：用 PyMuPDF 抽文本，并渲染每页 PNG 预览（PDF 表格的文本顺序常被打乱，肉眼必须复核）。
2. **结构识别**：在预览图上识别品牌分组（"真真真奶茶"、"招牌椰椰"…）、饮品行、杯型列、5 列做法/糖度（制作/七分糖/半糖/三分糖/温度）。
3. **字段映射**（参见 `references/excel-format.md`）：
   - A 配方版本号：从文件名/PDF 头部推断（默认"全国北京路通用"）。
   - B 饮品名称：饮品行原名（如"杨枝甘露（轻盈版）"）。
   - C 杯型：从 PDF 的"杯型"列推导（"大杯 热：22A纸杯/冰：700塑杯" → Excel 用"大杯"）。
   - D 做法：PDF 不显式给出，默认留空。
   - E 温度：见 PDF 温度列；"全温度"→5 项；"冰/少冰/去冰"→3 项。
   - F 糖度：标准糖度/七分糖/五分糖/三分糖/不额外加糖。
   - G 原料名称、H 原料用量：解析制作行，按"+"拆分；用量取数字（克/ml 同值）。
   - I 原料排序：默认留空。
4. **组合展开**：
   - 每款饮品 × 温度 × 糖度 → 多行原料。
   - "不额外加糖"组合 → 省略 蔗糖行。
   - 温度顺序固定：正常冰、少冰、去冰、常温、热（仅当 PDF 允许）。
   - 糖度顺序固定：标准糖度、七分糖、五分糖、三分糖、不额外加糖。
5. **用量归一化**：
   - 取整数（PDF 已经是整数）。
   - 写入 Excel 时转成字符串（与模板一致：H 列为字符串型数字）。
6. **生成 Excel**：使用 `openpyxl` 直接写新文件，**不要修改用户的源 PDF 或模板 Excel**。表头第 1 行 9 列固定；样式沿用模板默认（不设填充/边框/字体）。
7. **输出位置**：写到用户指定的目录（默认 `~/Desktop/openclaw/`，文件名 `<PDF 文件名>-导入配方.xlsx`）。

## Important Rules

- **物料名（2026-08 更新）**：**以配方正文（制作配方）中的名称为准**。仅保留确认过的映射：`蔗糖糖浆`→`蔗糖`、`凤梨果酱`→`凤梨果浆`；其他一律按正文原文写入（如 `石榴汁`、`速冻荔枝浆`、`厚椰乳`），不用标签表/物料对照表的叫法替换。完整词典见 `references/material-dictionary.md`，缺省再向用户确认。**新出现的物料生成后要补进词典**（2026-08-12 用户规则）。
- **相似词必须确认（2026-08-12 用户规则）**：当物料名与词典现有条目**相似/易混**（如同物异名、近似写法、词典有多个相似变体）时，**必须停下来找用户确认用哪个**，不得自行选择。典型情况：`速冻草莓果酱` vs `速冻草莓酱`、`四季春茶` vs `四季春茶汤`、`水蜜桃果酱` vs `速冻水蜜桃果酱`、`红西柚粒罐头` vs `红西柚颗粒罐头`、`桃果酱` vs `桃果肉饮料`、标签表叫法与正文叫法不一致等。
- **黄金冻 / 红西柚颗粒罐头 / 类似非机器出料**：当前模板**不包含**这些物料（手工加料）。生成时**不写入** Excel，除非用户明确要求加入。排除清单：黄金冻、茶冻、黑糖珍珠、椰果、粉色原味晶球、芋圆、黑糖冻、葡萄颗粒/红西柚颗粒罐头、罐头果粒、鲜果片（柠檬/橙/西瓜块）、冰块、过滤水/水、冰淇淋及圣代原料等。
- **七分糖 = 五分糖**：PDF 只列出"七分糖/半糖"和"三分糖"两档，但 Excel 还要"五分糖"。**默认把七分糖的 蔗糖用量复制给五分糖**（与模板现有数据一致）。
- **不额外加糖**：Excel 出现，但 PDF 没有显式标注；默认按"无 蔗糖 行"展开（参照模板）。PDF 注明：除冰淇淋、圣代、冰沙类产品外，其他产品均可制作不额外加糖。
- **杯型**（2026-08 实测修正）：PDF 杯型列常不写"大杯/中杯"而是写杯型描述，按描述判断：`咖啡专用杯`/`16A纸杯`=**中杯**；`22A纸杯`/`700塑杯`/`700磨砂注塑杯`/`22A荔枝纸杯`=**大杯**；PDF 明确写中杯/大杯按原文；若 PDF 同时给中杯/大杯两套配方则各生成一组（参考 `杨枝甘露椰椰（大杯/中杯）`、`黑糖珍珠奶茶(大杯/中杯)`）。
- **配方版本号**：**用户未指定时统一用 `全国北京路通用`**（2026-08 用户明确规则）。若 PDF 头部或文件名含渠道标识（"全国、商场"→ `全国标准`/`标准`；"线上"→ `线上产品`；"北京路"→ `全国北京路通用`）且用户认可，按其填写。
- **温度范围**：以 PDF 的"温度"列为准，不要凭空补"常温/热"。
- **样式**：模板无任何自定义样式（仅默认等线 11、无边框、无填充、无冻结、无下拉）；保持同样。
- **不要打开用户源 PDF 或 Excel 做原地编辑**：新文件写到输出目录。
- **多页 PDF**：PDF 可能是多页（如荔枝系列 10 页），只处理"前场配料"页面中的饮品表；"后场配料"（原料制备说明）与"标签配料表解读"不生成配方行。
- **同品多行（杯型/温度分列）**：部分产品（杨枝甘露椰椰、桑葚酸奶昔、黑糖珍珠奶茶等）同一饮品在 PDF 中有"中杯/大杯"或"冰/热"多行配方，需分别按行展开。冰沙类（牛油果芒芒甘露、波波多肉葡萄）的"单杯/双杯"、"杯中/冰沙杯"两段通常非机器出料，直接跳过或与用户确认。

## Quality Checklist

生成后逐项核对：
- [ ] 第 1 行表头为 9 列原文（不可改英文字段名 `version/drinkName/cup size/addtional/ice level/sugar level/materialName/materialDosage/materialOutSort`）。
- [ ] 每款饮品的 (温度 × 糖度) 组合数 = PDF 允许的 温度数 × 5 糖度。
- [ ] "不额外加糖"组合**不包含** 蔗糖行。
- [ ] 用量为字符串数字（`str(int)`）。
- [ ] 文件可被 Excel/WPS 直接打开，无任何非法字符。

## Resources

### scripts/
- `generate_import_xlsx.py`：从 PDF 路径生成 Excel 的端到端脚本（输入：PDF 路径 + 可选输出路径）。

### references/
- `excel-format.md`：9 列导入配方模板完整字段说明（生成目标格式）。
- `pdf-mapping.md`：PDF 各列如何映射到 Excel 9 列的具体规则与示例。
- `material-dictionary.md`：原料名称词典（PDF 写法 → Excel 标准写法，57 种实测）。
- `exported-data-11col.md`：导出文件（配方数据.xlsx）11 列结构、版本号含义、判定"当前在用配方"的方法。

## 版本判定（2026-08 实测）

- 最新 PDF 中的饮品在导出 Excel 中通常同时存在于多个版本号下（v1.0/v2.0/全国标准/标准/线上产品/全国北京路通用…）。
- "全国标准" 与 "标准" = 全国/商场渠道当前在用；"线上产品" = 线上渠道；"全国北京路通用" = 北京路门店（荔枝系列、凤梨冰奶等）。
- 判定当前在用方案：以最新 PDF（含更新日期）为基准，优先匹配 全国标准/标准 版本，数值一致者即当前在用；香港/韩国/USA 后缀为海外版，不用。
- 生成 9 列导入配方时 A 列版本号：按 PDF 渠道写（全国→`全国标准`、商场→`标准`、线上→`线上产品`、北京路→`全国北京路通用`；默认历史模板用过 `全国北京路通用`，生成时向用户确认渠道）。

## Pitfalls Observed (2026-08)

1. 本机 `editor_sdk` MCP 通道在本环境无法消费 `/stream`（持续返回 "No workbook open"），生成 Excel 时**不要走 tencent-local-office-edit**，直接用 `openpyxl` 写文件。
2. PyMuPDF `import fitz` 已 deprecated（仍有 1.28.2 可用，但会告警）；未来切 `import pymupdf`。
3. PDF 表格抽文本会按"列主序"乱排，**必须配合 PNG 预览**复核饮品顺序和原料拆分。